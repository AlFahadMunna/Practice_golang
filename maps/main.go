package main

// maps -> hash,object,dict
func main() {
	// creating map

	// setting an element
	// m := make(map[string]string)

	// m["name"] = "golang"
	// m["area"] = "backend"
	// fmt.Println(m["name"], m["area"])
	// fmt.Println(m["phone"])

	// fmt.Println(len(m))

	// delete(m, "area")
	// clear(m)
	// fmt.Println(m)

	// m := map[string]int{"price": 40, "phone": 3}

	// first element value second element true or false
	// m1 := map[string]int{"price": 40, "phone": 3}

	// fmt.Println(maps.Equal(m, m1))
}
